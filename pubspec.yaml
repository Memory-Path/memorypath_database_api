name: memorypath_db_api
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  hive: ^1.4.4+1
  hive_flutter: ^0.3.1
  json_serializable: ^4.0.2
  json_annotation: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: ^0.8.2
  build_runner: ^1.11.5

